package controller;

/**
 * An interface for all the interactions of running the program.
 */
public interface ImageController {
  /**
   * Runs a program that can load, process, and save images.
   */
  void run();
}
